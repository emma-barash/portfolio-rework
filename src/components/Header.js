import React from 'react';
import { HeaderContainer } from '../elements/index.js';

const Header = () => {
    return (
        <HeaderContainer>
            Hola Header
        </HeaderContainer>
    );
};

export default Header;